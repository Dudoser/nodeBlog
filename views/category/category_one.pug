h2 Articles
a(href="/") home
hr
h3 category #{post.title}
hr
each val in post.posts
    p
        span #{val.image}
        span  |
        span
            a(href='/post/' + val.url) #{val.title}
        span  |
        span   #{val.createdAt}
        span  |
        span #{val.user.name}